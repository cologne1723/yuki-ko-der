# 번역·검수 참여

코드와 번역은 한 저장소에서 관리합니다. 문제 하나나 짧은 수정부터 PR로 제안해 주세요.
검수 기록은 참여자가 남기고, 최종 PR 검토와 병합은 관리자가 담당합니다.
CI 통과나 PR 병합 자체는 사람 검수 승인이 아닙니다.

## 웹에서 참여

1. [공개 번역](https://cologne1723.github.io/yuki-ko-der/)과 해당 페이지의 일본어 원문을 확인합니다.
2. **GitHub에서 번역 수정**으로 문제 MDX를 편집하고 PR을 만듭니다.
3. PR에는 문제 번호, 변경 이유, 확인한 범위와 남은 의문을 적습니다.
4. 검수만 참여할 수도 있습니다. 최종 번역을 실제로 확인했다면 `humanReview`에 본인의 프로젝트 ID를 추가하고 PR을 만듭니다.

공개 페이지는 이미 배포된 번역입니다. PR의 수정본이 자동으로 미리보기 되는 것은 아닙니다.
미병합 변경은 GitHub diff로 확인하고, 수식·레이아웃까지 확인하려면 아래 로컬 도구를 사용하세요.

## 로컬 검수 도구 — 설치 가능하면 권장

Node.js 24, pnpm 11.21.0을 준비하고 자신의 fork를 clone한 뒤 실행합니다.

```sh
pnpm install --frozen-lockfile
pnpm review
```

검수 화면에서 원문을 내려받고 일본어·한국어·번역 소스를 비교합니다.
**검수자 ID**에 본인의 프로젝트 식별자를 입력합니다. ID는 브라우저에 기억되며 GitHub 계정 인증을 뜻하지 않습니다.
저장만 하면 승인되지 않습니다. 확인을 끝낸 뒤 **검수 승인**을 누르면 본인의 ID가 추가됩니다.
**내 검수 승인 취소**는 해당 ID만 제거합니다. 자동으로 다음 미검수 문제로 이동하는 옵션도 있습니다.
로컬 저장은 GitHub에 올라가지 않으므로 변경 파일을 commit하고 PR을 제출하세요.

## 검수 기록

```yaml
humanReview: [cologne, reviewer2]
machineReview: unreviewed
```

- `humanReview: []`는 사람 미검수입니다. 한 명 이상 있으면 사람 검수를 받은 번역입니다.
- ID는 프로젝트 식별자이며 빈 값·중복은 허용하지 않습니다. 타인의 검수를 추측해서 기록하지 마세요. 관리자가 PR에서 기재를 확인합니다.
- 기존 사람 승인 기록은 `cologne`로 이전했습니다. 신규 승인에 자동으로 사용하는 기본 ID가 아닙니다.
- 목록은 **현재 번역에 대한 검수**입니다. 본문·제목·대응 원문을 바꾸면 이전 ID와 기계 승인을 초기화하고, 바뀐 내용을 실제 재검수한 ID만 다시 추가하세요. 로컬 도구는 저장 시 초기화합니다.
- 공개 여부나 검수자 기록만 변경해도 다른 검수자의 기록은 유지합니다. 과거 검수 이력은 Git 기록에서 확인합니다.
- 기계 검수와 사람 검수는 별개입니다. 미검수 번역도 기존 정책대로 안내와 함께 공개될 수 있으며, `visibility: false`는 별도로 공개를 제외합니다.

작업 전 [번역 워크플로](problem-translations/WORKFLOW.md)와 [검토 워크플로](problem-translations/REVIEW_WORKFLOW.md)를 참고하세요.
원문이나 용어가 불확실하면 승인하지 말고 PR에 남겨 주세요.
