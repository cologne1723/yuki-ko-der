import { readFile, writeFile } from "node:fs/promises";
const p = "/Users/hyea/yukicoder/problem-translations/ko/problems/21.html";
let s = await readFile(p, "utf8");
s = s
  .replace(
    "\\(N\\)개の숫자が주어집니다のでこれらを\\(K(\\geq\n            3)\\)개のグループに振り分ける。",
    "\\(N\\)개의 숫자가 주어지므로 이를 \\(K(\\geq 3)\\)개의 그룹으로 나눕니다.",
  )
  .replace(
    "ただし各グループには最低一개숫자が含まれているとする。",
    "단, 각 그룹에는 최소 한 개의 숫자가 포함되어야 합니다.",
  )
  .replace("ex) 例えば 与えれる숫자が", "예) 예를 들어 주어지는 숫자가")
  .replace("のような振り分けかたはただしく", "와 같이 나누는 것은 올바르고")
  .replace(
    "のような振り分けかたは認められません",
    "와 같이 나누는 것은 허용되지 않습니다",
  )
  .replace(
    "グループごとに평균을계산し, それらをもとに 최대의평균 - 최소의평균\n            を계산し、",
    "그룹마다 평균을 계산하고, 이를 바탕으로 최댓값 평균 - 최솟값 평균을 계산하여,",
  )
  .replace(
    "最後に小数点이하を切り上げその값を「평균の차이」と呼ぶ。",
    "마지막으로 소수점 이하를 올림한 값을 「평균의 차이」라고 부릅니다.",
  )
  .replace(
    "평균의차이を最も大きくするようなグループ分けをしたとき、평균의차이はいく개になるか答えよ。",
    "평균의 차이가 최대가 되도록 그룹을 나눌 때, 평균의 차이는 얼마인지 답하세요.",
  )
  .replace(
    "\\(1\\)번째 줄에는\\(N\\ (3 \\leq N \\leq 9)\\)が주어집니다。",
    "\\(1\\)번째 줄에는 \\(N\\ (3 \\leq N \\leq 9)\\)가 주어집니다.",
  )
  .replace(
    "\\(2\\)번째 줄에는\\(K\\ (3 \\leq K \\leq N)\\)が주어집니다。",
    "\\(2\\)번째 줄에는 \\(K\\ (3 \\leq K \\leq N)\\)가 주어집니다.",
  )
  .replace(
    "\\(3\\)번째 줄～\\(N+2\\)번째 줄에는숫자\\((1 \\leq n_i \\leq 1000, 1 \\leq\n            i \\leq N)\\)が주어집니다。",
    "\\(3\\)번째 줄부터 \\(N+2\\)번째 줄까지 숫자 \\(n_i\\ (1 \\leq n_i \\leq 1000, 1 \\leq i \\leq N)\\)가 주어집니다.",
  )
  .replace("答えの수치を문자열で출력してください。", "답인 수치를 출력하세요.")
  .replace(
    "例えば \\({\\{555\\}, \\{21,20\\}, \\{433,301\\}\\})",
    "예를 들어 \\({\\{555\\}, \\{21,20\\}, \\{433,301\\}\\})",
  )
  .replace("のようにグループ分けすると", "와 같이 그룹을 나누면")
  .replace("평균は", "평균은")
  .replace("なので", "이므로")
  .replace("최대의평균 - 최소의평균は", "최댓값 평균 - 최솟값 평균은")
  .replace("最後に小数点이하を切り上げて", "마지막으로 소수점 이하를 올림하면");
await writeFile(p, s);
